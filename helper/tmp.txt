нужно добавить до описания проверку на шифрование
если шифровано показываем замочек и описание зашифровано
при нажатии замочек появляется поле ввода пароля с глазом
на той же строчке с замочком и кнопочка для получения расшифрованного описания
при получении описания показываем описание при нажатии кнопки замочек описание
стирается и скрывается показывается замочек и описание зашифровано

block_detail.html
{% extends "base.html" %}
{% load static %}

{% block title %}Блок: {{ m_block.title }}{% endblock %}

{% block extra_css %}
<link rel="stylesheet" href="{% static 'css/block_detail.css' %}">
{% endblock %}

{% block content %}
<div class="container my-4">

    <!-- HEADER -->
    <div class="card shadow-sm mb-4">
        <div class="card-body d-flex justify-content-between align-items-center">
            <div>
                <h3 class="mb-1">{{ m_block.title }}</h3>
                <small class="text-muted">
                    Создано: {{ m_block.created_at|date:"d M Y H:i"|default:"-" }} |
                    Обновлено: {{ m_block.updated_at|date:"d M Y H:i"|default:"-" }}
                </small>
            </div>
        </div>
    </div>

    <!-- TASKS LIST -->
    <div class="row g-3">
        {% for bt in block_tasks %}
        <div class="col-md-6 col-lg-4">
            <div class="card h-100 shadow-sm task-card">
                <div class="card-body d-flex flex-column align-items-center text-center">

                    {% if bt.icon %}
                        <img src="{{ bt.icon }}" class="task-icon mb-3 rounded" alt="{{ bt.title }}">
                    {% else %}
                        <div class="placeholder-icon mb-3"></div>
                    {% endif %}

                    <h5 class="card-title">{{ bt.title }}</h5>
                    <p class="card-text text-muted small">{{ bt.description }}</p>

                    <ul class="list-unstyled text-start w-100 mt-auto">
                        <li><strong>Количество:</strong> {{ bt.amount }}</li>
                        <li><strong>Время:</strong> {{ bt.time }}</li>
                    </ul>

                </div>
            </div>
        </div>
        {% empty %}
        <div class="col-12">
            <div class="alert alert-info">В этом блоке пока нет задач</div>
        </div>
        {% endfor %}
    </div>

</div>
{% endblock %}
